# 🧩 RESTful API Design
## ระบบจองห้องประชุมออนไลน์ (Meeting Room Booking System)

รายงานฉบับนี้เป็นการออกแบบ RESTful API สำหรับระบบจองห้องประชุมออนไลน์  
เพื่อรองรับการใช้งานของนักศึกษา อาจารย์ และเจ้าหน้าที่  
โดยออกแบบตามหลัก REST และสอดคล้องกับ Business Rules ที่กำหนด

---

## 🔧 Design Principles

- ใช้ **RESTful Architecture**
- ใช้ HTTP Methods ให้เหมาะสมกับการทำงาน (GET, POST, PUT, DELETE, PATCH)
- ใช้ URL Naming แบบ **Resource-based**
- รับ–ส่งข้อมูลในรูปแบบ **JSON**
- รองรับ Role-based Access (User / Staff)

---

## 📦 Main Resources

- **Rooms** – ข้อมูลห้องประชุม
- **Bookings** – ข้อมูลการจอง
- **Users / Auth** – การยืนยันตัวตนและข้อมูลผู้ใช้

---

## 🏢 Rooms API

ใช้สำหรับจัดการข้อมูลห้องประชุม (เจ้าหน้าที่เท่านั้นสำหรับการแก้ไข)

| HTTP Method | Endpoint | Description | Request Body | Response (Success) | Status Code |
|------------|---------|------------|-------------|------------------|-------------|
| GET | `/api/rooms` | ดึงรายการห้องประชุมทั้งหมด | - | `[{ room_id, name, capacity }]` | 200 |
| GET | `/api/rooms/:id` | ดึงข้อมูลห้องประชุมรายห้อง | - | `{ room_id, name, capacity }` | 200 |
| POST | `/api/rooms` | เพิ่มห้องประชุมใหม่ (Staff) | `{ name, building, floor, capacity, facilities }` | `{ room_id, ... }` | 201 |
| PUT | `/api/rooms/:id` | แก้ไขข้อมูลห้องประชุม (Staff) | `{ name, capacity }` | `{ updated_room }` | 200 |
| DELETE | `/api/rooms/:id` | ลบห้องประชุม (Staff) | - | `{ message: "deleted" }` | 200 |

---

## 📅 Bookings API

ใช้สำหรับจัดการการจองห้องประชุมของผู้ใช้

| HTTP Method | Endpoint | Description | Request Body | Response (Success) | Status Code |
|------------|---------|------------|-------------|------------------|-------------|
| GET | `/api/bookings` | ดูรายการจองทั้งหมด (Staff) | - | `[{ booking }]` | 200 |
| GET | `/api/bookings/my` | ดูการจองของผู้ใช้เอง | - | `[{ booking }]` | 200 |
| POST | `/api/bookings` | สร้างการจองใหม่ | `{ room_id, booking_date, start_time, end_time, purpose }` | `{ booking }` | 201 |
| DELETE | `/api/bookings/:id` | ยกเลิกการจอง | - | `{ message: "cancelled" }` | 200 |
| PATCH | `/api/bookings/:id/approve` | อนุมัติ/ปฏิเสธการจอง (Staff) | `{ status }` | `{ booking }` | 200 |

---

## 👤 Authentication & Users API

ใช้สำหรับจัดการบัญชีผู้ใช้งาน

| HTTP Method | Endpoint | Description | Request Body | Response (Success) | Status Code |
|------------|---------|------------|-------------|------------------|-------------|
| POST | `/api/auth/register` | สมัครสมาชิก | `{ name, email, password, role }` | `{ user_id }` | 201 |
| POST | `/api/auth/login` | เข้าสู่ระบบ | `{ email, password }` | `{ token }` | 200 |
| GET | `/api/users/profile` | ดูข้อมูลผู้ใช้ที่เข้าสู่ระบบ | - | `{ user }` | 200 |

---

## 🧪 Example: Create Booking API

### POST `/api/bookings`

**Request Body**
```json
{
  "room_id": 101,
  "booking_date": "2026-01-15",
  "start_time": "09:00",
  "end_time": "12:00",
  "purpose": "ประชุมโครงงาน"
}

Response (201 Created)
{
  "booking_id": 1234,
  "room_id": 101,
  "user_id": 567,
  "status": "pending",
  "created_at": "2026-01-08T10:30:00Z"
}

